# ARCHITECTURE.md

> 逆向对象：`clipboard-20260821-150752.935345-000001.apk`
> 包名：`com.autoclicker.app` ｜ 应用名：连点器（auto-clicker）
> minSdk=24 / targetSdk=34（Android 7.0 ~ 14.0）

---

## 一、证据结论总览

### 1. 权限声明（AndroidManifest.xml，反编译确认 ✔）

- `android.permission.SYSTEM_ALERT_WINDOW` —— 悬浮窗
- `android.permission.FOREGROUND_SERVICE` —— 前台服务（保活）
- **`android.permission.BIND_ACCESSIBILITY_SERVICE`（无障碍服务铁证 ✔）**，挂在 `AutoClickService` 上，并注册 `<action android:name="android.accessibilityservice.AccessibilityService"/>` 意图过滤器 + `meta-data accessibility_service_config`

### 2. 五大关键类/方法搜索结论（仅类名/方法名校验）

| # | 目标 | 结论 |
|---|------|------|
| 1 | `AccessibilityService`（核心服务） | ✅ 已找到 — 自定义类 `AutoClickService` 继承，并依赖 `AccessibilityService$GestureResultCallback` |
| 2 | `dispatchGesture`（模拟手势点击） | ✅ 已找到 — 完整手势链路：`GestureDescription$Builder` + `Path`/`lineTo` + `GestureDescription$StrokeDescription` + `dispatchGesture` |
| 3 | `WindowManager`（悬浮窗管理） | ✅ 已找到 — `android/view/WindowManager` + `WindowManager$LayoutParams`（悬浮窗服务 `FloatingWidgetService`） |
| 4 | `SharedPreferences`（配置存储） | ✅ 已找到 — `android/content/SharedPreferences` 及其 `$Editor`，调用 `getSharedPreferences`、`putString`、`apply`、`loadDefaults`/`savePoints` |
| 5 | `Settings.canDrawOverlays`（悬浮窗权限适配） | ✅ 已找到 — 调 `canDrawOverlays` 检查 + 跳 `android.settings.action.MANAGE_OVERLAY_PERMISSION` 引导授权 |

### 3. 控制方式判定

**悬浮窗控制**（非通知栏）。

证据：`floating_widget.xml` 中 `FloatingWidgetService` 通过 `WindowManager.addView/updateViewLayout/removeView` 渲染一个常驻悬浮球，内含 `开始/停止` 按钮（contentDescription="开始/停止"）、状态文案（`○ 就绪`/`● 连点运行中`）、连点计数、关闭按钮；并由 `tv_status`（"已停止"）等实时反馈状态。主界面 `activity_main` 仅为引导页。

---

## 二、确证使用到的安卓原生 API

1. **无障碍自动点击** — 基于 `android.accessibilityservice.AccessibilityService`，通过 `GestureDescription`（`Path` + `StrokeDescription`）构造模拟手势，调用 `dispatchGesture()` 注入点击；配合 `canPerformGestures=true`（配置于 `res/xml/accessibility_service_config.xml`）、`GestureResultCallback` 回执。
2. **悬浮窗交互** — `WindowManager.addView()/updateViewLayout()/removeView()` + `WindowManager.LayoutParams`（TYPE_APPLICATION_OVERLAY），实现悬浮球；用 `Settings.canDrawOverlays()` 校验并在 `ACTION_MANAGE_OVERLAY_PERMISSION` 引导授权。
3. **配置持久化** — `SharedPreferences`（`SharedPreferences.Editor.putString/apply`）保存点位序列 `targets=`、间隔、模式等。
4. **定时循环** — `Handler.postDelayed()` 驱动定时器 + `setInterval` 实现指定间隔的循环点击逻辑。
5. **系统事件联动** — 注册 `android.intent.action.SCREEN_OFF` 广播实现息屏自动停止；`PositionPickerActivity` 用 `onTouchEvent` + `getRawX/getRawY` 获取屏幕绝对坐标。

---

## 三、逻辑功能描述

> 基于无障碍服务开发的智能连点器：主界面引导开启无障碍与悬浮窗权限，随后启动悬浮球服务，用户可在悬浮球上启停连点。通过 `AccessibilityService` 构造 `GestureDescription` 并调用 `dispatchGesture()` 模拟点击，坐标由 `onTouchEvent` 捕获并经 `SharedPreferences` 序列化持久化；由 `Handler.postDelayed` 按设定间隔循环注入点击，支持单点、多点排序、次数限制与间隔随机变化，`SCREEN_OFF` 广播实现息屏自动停止。整体为"无障碍注入点击 + 悬浮窗交互 + 本地持久化"的轻量架构。

---

## 附：关键技术点校验明细
- `BIND_ACCESSIBILITY_SERVICE`：`<service android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">` ✔
- `dispatchGesture` 前提 `canPerformGestures="true"`：在 `accessibility_service_config.xml` 确认 ✔
- 悬浮窗 res 资源：`floating_widget` / `floating_bg` / `btn_circle_bg` ✔
- 权限申请：无危险权限网络/定位；仅悬浮窗（SYSTEM_ALERT_WINDOW）+ 前台服务。
